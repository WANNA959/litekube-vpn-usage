mode: client
client:
  # vpn tls cert root dir
  vpnCertDir: /root/go_project/litekube-vpn-usage/certs/test2/
  # vpn server ip
  serverAddr: 101.43.253.110
  # vpn server port
  port: 6441
  # MTU
  mtu: 1400
  # node token
  token: c9168a88d15b4a18
  # if redict gateway
  redirectGateway: false