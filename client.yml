mode: client
client:
  # newwork tls cert root dir
  networkCertDir: /root/go_project/network-controller/certs/test2/
  # network server ip
  serverAddr: 101.43.253.110
  # network server port
  port: 6441
  # MTU
  mtu: 1400
  # node token
  token: c9168a88d15b4a18
  # if redict gateway
  redirectGateway: false